"""Camada de vídeo: TTS com timestamps + montagem do props.json pro Remotion.

O pipeline (Python) escreve um props.json determinístico; o Remotion (subprojeto
JS em ./video) consome e renderiza o .mp4 vertical 9:16. Ver DESIGN.md §7-bis.
"""
